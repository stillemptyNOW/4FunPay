# Зависимости 4FunPay. Python 3.11+
#
# Установка:  pip install -U -r requirements.txt

# --- Работа с FunPay --------------------------------------------------------
# HTTP-запросы к funpay.com. В исходном проекте версия была закреплена
# на 2.28.1 (релиз 2022 года); закрепление снято, чтобы приходили
# исправления безопасности.
requests>=2.32.0
# Multipart-загрузка изображений в чат и в лоты.
requests_toolbelt>=1.0.0
# Разбор HTML-страниц FunPay.
beautifulsoup4>=4.12.0
# Парсер для BeautifulSoup: быстрее встроенного и терпимее к битой разметке.
lxml>=5.3.0
# Поддержка SOCKS5-прокси в requests.
pysocks>=1.7.1

# --- Telegram ---------------------------------------------------------------
# Версия закреплена намеренно: панель управления опирается на конкретный
# набор методов (set_my_description, get_my_short_description и другие).
# Перед подъёмом версии проверь работу меню целиком.
pytelegrambotapi==4.15.2

# --- Прочее -----------------------------------------------------------------
# Хеширование пароля панели управления.
bcrypt>=4.2.0
# Цветной вывод в консоль.
colorama>=0.4.6
# Сводка по загрузке системы для команды /sys.
psutil>=5.9.4
# Обработка изображений при загрузке в чат и в лоты.
pillow>=10.3.0
# Чтение файла .env. Не обязателен: при отсутствии пакета используется
# встроенный минимальный разбор (см. Utils/secrets.py).
python-dotenv>=1.0.0
